  你回家走「刪庫重匯」可以，mac 指令：  
  
```bash
  mysql -u root -p -e "DROP DATABASE IF EXISTS moducore_platform; CREATE DATABASE moducore_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;" 
  mysql -u root -p moducore_platform < ~/Downloads/dinecore_full_2026-03-05.sql     
```
